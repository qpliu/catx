#!/bin/sh
set -e
pwdpwd=`pwd`
for a in test/*/get ../src/*/get
do
    b=`dirname "$a"`
    cd "$b"
    echo "$b"
    ./get
    if test -e ../buildBin
    then
	../buildBin
    fi
    cd "$pwdpwd"
done
